<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorBackgroundLight</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TextHeadingTheme">
        <item name="android:textSize">@dimen/dimen_heading_text_size_standard</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="TextStandardTheme">
        <item name="android:textSize">@dimen/dimen_text_size_standard</item>
        <item name="android:padding">@dimen/dimen_text_padding_small</item>
    </style>
</resources>